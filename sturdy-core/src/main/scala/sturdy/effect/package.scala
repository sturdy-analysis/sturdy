package sturdy

/** Package [[effect]] provides interfaces and instances for common effects in programming languages
 *
 * Each effect implements the interface [[Effect]], which allows to retrieve and set the internal state of the effect.
 * [[EffectStack]] combines multiple effects and is required by the fixpoint algorithm.
 */
package object effect {}
