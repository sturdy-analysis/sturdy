nested(x) {
  return x + 2;
}


id(x) {
  return nested(x);
}

main() {
  var i, r1, r2, r3;
  r1 = id(42);
  assert(r1 == 44);
  i = input;
  r2 = i;
  r3 = id(i);
  assert(r1 == 44);
  assert(r2 == i);
  assert(r3 == i + 2);
  return 0;
}

