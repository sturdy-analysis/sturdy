id(x) {
  var r;
  if(1 > x) {
    r = x;
  } else {
    r = id(x-1) + 1;
  }
  return r;
}

main() {
  var r1, i, r2;
  r1 = id(42);
  assert(r1 == 42);
  i = input;
  r1 = i;
  r2 = id(i);
  assert(r1 == i);
  assert(r2 == i);
  return 0;
}

