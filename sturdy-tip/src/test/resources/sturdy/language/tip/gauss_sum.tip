sum(x){
    var y;
    if(0 >= x) {
        y = 0;
    } else {
        y = x + sum(x-1);
    }
    return y;
}

main() {
    var x, y;
    x = input;
    if(x >= 0) {
      y = sum(x);
      assert(y >= x);
    }
    return 0;
}