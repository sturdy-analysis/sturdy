incr(i, bound) {
  var r;
  if(i+1 > bound) { r = i; }
  else { r = incr(i+1, bound); }
  return r;
}

main() {
  var r1, bound, r2;
  r1 = incr(0, 42);
  assert(r1 == 42);
  bound = input;
  r2 = incr(0, bound);
  assert(bound+1 > r2);
  return 0;
}