// [0,inf] -> [1,inf]
rec(n) {
    var f;
    if (1 > n) { f=1; }
    else { f=n*rec(n-1); }
    output f;
    return f;
}

foo(n) {
	var x;
	x = 3*n;
	if (x == 0) { x = rec(n); }
	else { x = foo(x) + foo(x-1); }
	output(rec(n));
	return x;
}

main() {
    var n;
    n = input;
    return foo(n);
}