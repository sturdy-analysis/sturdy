filter(x) {
  var y;
  if (x < 0)
    y = x / 0;
  else
    y = 1;
  return y;
}

main() {
  var r1, i, r2;
  r1 = id(42);
  assert(r1 == 42);
  i = input;
  r1 = i;
  r2 = id(i);
  assert(r1 == i);
  assert(r2 == i);
  return 0;
}

