main() {
    var b,x,y,z,r;
    z = input;
    x = z;
    y = input;
    if (x > y) {
      b = alloc x;
      // assert *b > y;
      if (*b > y) {
        r = 1;
      } else {
        r = 0;
      }
    } else {
      r = 5;
    }
    assert (r > 0);
    return r;
}
