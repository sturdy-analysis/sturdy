main(){
    var a,b;
    a = "Hello";
    b = "World!";
    c = (a.concat(b));

    //concat(a,b) => Exp.StringConcat(Parse(a), Parse(b))
    //concat(a,b) => Exp.Call("concat", [Parse(a), Parse(b)])
    return c;
}
