main() {
    var x,y,y1,y2,y3;
    x = 0;
    y = alloc x;
    if (input) {
      x = 1;
      y1 = alloc x;
    } else {
      x = 2;
      y1 = alloc x;
    }
    y2 = alloc x;
    x = 3;
    y3 = alloc x;
    // assert (*y == 0)
    // assert (*y1 > 0)
    // assert (2 > *y1)
    // assert (*y1 == *y2)
    // assert (*y3 == 3)
    return *y2;
}
