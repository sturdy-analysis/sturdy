fib(n) {
    var res;
    if (n == 1) {
        res = 1;
    } else {
        res = fib(n - 2) + fib(n - 1);
    }
    return res;
}


f(x) {
    var r;
    if (x == input) {
        r = 1;
    } else {
        r = fib(10) + fib(x);
        r = r + x * f(x - 1);
        r = r + 1;
    }
    return r;
}

main() {
    return f(10);
}
