peanoAddition(a, b) {
  var r;
  if(1 > a) { r = a + b; }
  else { r = peanoAddition(a-1, b+1); }
  return r;
}

main() {
  var r1, x, y, r2;
  r1 = peanoAddition(2, 3);
  assert(r1 == 5);
  x = input;
  y = input;
  r2 = peanoAddition(x, y);
  assert(r2 == x + y);
  return 0;
}

