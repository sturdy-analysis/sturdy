g(n) {
    var res;
    if (n == 1) {
        res = 1;
    } else {
        res = f(n) + g(n - 1);
    }
    return res;
}


f(x) {
    var r;
    if (x == input) {
        r = 1;
    } else {
        r = g(x) + g(x+1) + g(x+2);
    }
    return r;
}

main() {
    return f(10);
}
