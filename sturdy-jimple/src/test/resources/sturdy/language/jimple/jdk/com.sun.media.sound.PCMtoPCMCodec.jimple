public final class com.sun.media.sound.PCMtoPCMCodec extends com.sun.media.sound.SunCodec
{
    private static final javax.sound.sampled.AudioFormat$Encoding[] inputEncodings;
    private static final javax.sound.sampled.AudioFormat$Encoding[] outputEncodings;
    private static final int tempBufferSize;
    private byte[] tempBuffer;

    public void <init>()
    {
        com.sun.media.sound.PCMtoPCMCodec r0;
        javax.sound.sampled.AudioFormat$Encoding[] $r1, $r2;

        r0 := @this: com.sun.media.sound.PCMtoPCMCodec;

        $r2 = <com.sun.media.sound.PCMtoPCMCodec: javax.sound.sampled.AudioFormat$Encoding[] inputEncodings>;

        $r1 = <com.sun.media.sound.PCMtoPCMCodec: javax.sound.sampled.AudioFormat$Encoding[] outputEncodings>;

        specialinvoke r0.<com.sun.media.sound.SunCodec: void <init>(javax.sound.sampled.AudioFormat$Encoding[],javax.sound.sampled.AudioFormat$Encoding[])>($r2, $r1);

        r0.<com.sun.media.sound.PCMtoPCMCodec: byte[] tempBuffer> = null;

        return;
    }

    public javax.sound.sampled.AudioFormat$Encoding[] getTargetEncodings(javax.sound.sampled.AudioFormat)
    {
        com.sun.media.sound.PCMtoPCMCodec r9;
        javax.sound.sampled.AudioFormat r0;
        javax.sound.sampled.AudioFormat$Encoding $r1, $r2, $r3, $r4, $r5, $r6;
        javax.sound.sampled.AudioFormat$Encoding[] $r7, $r8;
        boolean $z0, $z1;

        r9 := @this: com.sun.media.sound.PCMtoPCMCodec;

        r0 := @parameter0: javax.sound.sampled.AudioFormat;

        $r1 = virtualinvoke r0.<javax.sound.sampled.AudioFormat: javax.sound.sampled.AudioFormat$Encoding getEncoding()>();

        $r2 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $z0 = virtualinvoke $r1.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>($r2);

        if $z0 != 0 goto label1;

        $r5 = virtualinvoke r0.<javax.sound.sampled.AudioFormat: javax.sound.sampled.AudioFormat$Encoding getEncoding()>();

        $r6 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_UNSIGNED>;

        $z1 = virtualinvoke $r5.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>($r6);

        if $z1 == 0 goto label2;

     label1:
        $r8 = newarray (javax.sound.sampled.AudioFormat$Encoding)[2];

        $r3 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $r8[0] = $r3;

        $r4 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_UNSIGNED>;

        $r8[1] = $r4;

        return $r8;

     label2:
        $r7 = newarray (javax.sound.sampled.AudioFormat$Encoding)[0];

        return $r7;
    }

    public javax.sound.sampled.AudioFormat[] getTargetFormats(javax.sound.sampled.AudioFormat$Encoding, javax.sound.sampled.AudioFormat)
    {
        com.sun.media.sound.PCMtoPCMCodec r0;
        javax.sound.sampled.AudioFormat r1, $r5, $r6, $r7, $r10;
        java.util.Vector $r3;
        int $i0, $i1, $i2, i3, i4;
        boolean $z0;
        javax.sound.sampled.AudioFormat$Encoding $r8, r9;
        javax.sound.sampled.AudioFormat[] $r2, r11;
        java.lang.Object $r4;

        r0 := @this: com.sun.media.sound.PCMtoPCMCodec;

        r9 := @parameter0: javax.sound.sampled.AudioFormat$Encoding;

        r1 := @parameter1: javax.sound.sampled.AudioFormat;

        $r2 = specialinvoke r0.<com.sun.media.sound.PCMtoPCMCodec: javax.sound.sampled.AudioFormat[] getOutputFormats(javax.sound.sampled.AudioFormat)>(r1);

        $r3 = new java.util.Vector;

        specialinvoke $r3.<java.util.Vector: void <init>()>();

        i3 = 0;

     label1:
        $i0 = lengthof $r2;

        if i3 >= $i0 goto label3;

        $r7 = $r2[i3];

        $r8 = virtualinvoke $r7.<javax.sound.sampled.AudioFormat: javax.sound.sampled.AudioFormat$Encoding getEncoding()>();

        $z0 = virtualinvoke $r8.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>(r9);

        if $z0 == 0 goto label2;

        $r10 = $r2[i3];

        virtualinvoke $r3.<java.util.Vector: void addElement(java.lang.Object)>($r10);

     label2:
        i3 = i3 + 1;

        goto label1;

     label3:
        $i1 = virtualinvoke $r3.<java.util.Vector: int size()>();

        r11 = newarray (javax.sound.sampled.AudioFormat)[$i1];

        i4 = 0;

     label4:
        $i2 = lengthof r11;

        if i4 >= $i2 goto label5;

        $r4 = virtualinvoke $r3.<java.util.Vector: java.lang.Object elementAt(int)>(i4);

        $r5 = (javax.sound.sampled.AudioFormat) $r4;

        $r6 = (javax.sound.sampled.AudioFormat) $r5;

        r11[i4] = $r6;

        i4 = i4 + 1;

        goto label4;

     label5:
        return r11;
    }

    public javax.sound.sampled.AudioInputStream getAudioInputStream(javax.sound.sampled.AudioFormat$Encoding, javax.sound.sampled.AudioInputStream)
    {
        com.sun.media.sound.PCMtoPCMCodec r0;
        javax.sound.sampled.AudioFormat $r3, $r7, $r14, $r15;
        javax.sound.sampled.AudioInputStream r2, $r16;
        int $i0, $i1, $i2;
        java.lang.String $r8, $r11, $r13;
        boolean $z0, $z1;
        java.lang.IllegalArgumentException $r4;
        javax.sound.sampled.AudioFormat$Encoding r1;
        float $f0, $f1;
        java.lang.StringBuilder $r5, $r6, $r9, $r10, $r12;

        r0 := @this: com.sun.media.sound.PCMtoPCMCodec;

        r1 := @parameter0: javax.sound.sampled.AudioFormat$Encoding;

        r2 := @parameter1: javax.sound.sampled.AudioInputStream;

        $r3 = virtualinvoke r2.<javax.sound.sampled.AudioInputStream: javax.sound.sampled.AudioFormat getFormat()>();

        $z0 = virtualinvoke r0.<com.sun.media.sound.PCMtoPCMCodec: boolean isConversionSupported(javax.sound.sampled.AudioFormat$Encoding,javax.sound.sampled.AudioFormat)>(r1, $r3);

        if $z0 == 0 goto label1;

        $r14 = virtualinvoke r2.<javax.sound.sampled.AudioInputStream: javax.sound.sampled.AudioFormat getFormat()>();

        $r15 = new javax.sound.sampled.AudioFormat;

        $f0 = virtualinvoke $r14.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        $i0 = virtualinvoke $r14.<javax.sound.sampled.AudioFormat: int getSampleSizeInBits()>();

        $i1 = virtualinvoke $r14.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i2 = virtualinvoke $r14.<javax.sound.sampled.AudioFormat: int getFrameSize()>();

        $f1 = virtualinvoke $r14.<javax.sound.sampled.AudioFormat: float getFrameRate()>();

        $z1 = virtualinvoke $r14.<javax.sound.sampled.AudioFormat: boolean isBigEndian()>();

        specialinvoke $r15.<javax.sound.sampled.AudioFormat: void <init>(javax.sound.sampled.AudioFormat$Encoding,float,int,int,int,float,boolean)>(r1, $f0, $i0, $i1, $i2, $f1, $z1);

        $r16 = virtualinvoke r0.<com.sun.media.sound.PCMtoPCMCodec: javax.sound.sampled.AudioInputStream getAudioInputStream(javax.sound.sampled.AudioFormat,javax.sound.sampled.AudioInputStream)>($r15, r2);

        return $r16;

     label1:
        $r4 = new java.lang.IllegalArgumentException;

        $r5 = new java.lang.StringBuilder;

        specialinvoke $r5.<java.lang.StringBuilder: void <init>()>();

        $r6 = virtualinvoke $r5.<java.lang.StringBuilder: java.lang.StringBuilder append(java.lang.String)>("Unsupported conversion: ");

        $r7 = virtualinvoke r2.<javax.sound.sampled.AudioInputStream: javax.sound.sampled.AudioFormat getFormat()>();

        $r8 = virtualinvoke $r7.<javax.sound.sampled.AudioFormat: java.lang.String toString()>();

        $r9 = virtualinvoke $r6.<java.lang.StringBuilder: java.lang.StringBuilder append(java.lang.String)>($r8);

        $r10 = virtualinvoke $r9.<java.lang.StringBuilder: java.lang.StringBuilder append(java.lang.String)>(" to ");

        $r11 = virtualinvoke r1.<javax.sound.sampled.AudioFormat$Encoding: java.lang.String toString()>();

        $r12 = virtualinvoke $r10.<java.lang.StringBuilder: java.lang.StringBuilder append(java.lang.String)>($r11);

        $r13 = virtualinvoke $r12.<java.lang.StringBuilder: java.lang.String toString()>();

        specialinvoke $r4.<java.lang.IllegalArgumentException: void <init>(java.lang.String)>($r13);

        throw $r4;
    }

    public javax.sound.sampled.AudioInputStream getAudioInputStream(javax.sound.sampled.AudioFormat, javax.sound.sampled.AudioInputStream)
    {
        com.sun.media.sound.PCMtoPCMCodec r0;
        javax.sound.sampled.AudioInputStream r2, $r3;
        javax.sound.sampled.AudioFormat r1;

        r0 := @this: com.sun.media.sound.PCMtoPCMCodec;

        r1 := @parameter0: javax.sound.sampled.AudioFormat;

        r2 := @parameter1: javax.sound.sampled.AudioInputStream;

        $r3 = specialinvoke r0.<com.sun.media.sound.PCMtoPCMCodec: javax.sound.sampled.AudioInputStream getConvertedStream(javax.sound.sampled.AudioFormat,javax.sound.sampled.AudioInputStream)>(r1, r2);

        return $r3;
    }

    private javax.sound.sampled.AudioInputStream getConvertedStream(javax.sound.sampled.AudioFormat, javax.sound.sampled.AudioInputStream)
    {
        byte[] $r5;
        com.sun.media.sound.PCMtoPCMCodec r4;
        javax.sound.sampled.AudioFormat $r1, r2;
        javax.sound.sampled.AudioInputStream r0, r6;
        com.sun.media.sound.PCMtoPCMCodec$PCMtoPCMCodecStream $r3;
        boolean $z0;

        r4 := @this: com.sun.media.sound.PCMtoPCMCodec;

        r2 := @parameter0: javax.sound.sampled.AudioFormat;

        r0 := @parameter1: javax.sound.sampled.AudioInputStream;

        $r1 = virtualinvoke r0.<javax.sound.sampled.AudioInputStream: javax.sound.sampled.AudioFormat getFormat()>();

        $z0 = virtualinvoke $r1.<javax.sound.sampled.AudioFormat: boolean matches(javax.sound.sampled.AudioFormat)>(r2);

        if $z0 == 0 goto label1;

        r6 = r0;

        goto label2;

     label1:
        $r3 = new com.sun.media.sound.PCMtoPCMCodec$PCMtoPCMCodecStream;

        specialinvoke $r3.<com.sun.media.sound.PCMtoPCMCodec$PCMtoPCMCodecStream: void <init>(com.sun.media.sound.PCMtoPCMCodec,javax.sound.sampled.AudioInputStream,javax.sound.sampled.AudioFormat)>(r4, r0, r2);

        r6 = $r3;

        $r5 = newarray (byte)[64];

        r4.<com.sun.media.sound.PCMtoPCMCodec: byte[] tempBuffer> = $r5;

     label2:
        return r6;
    }

    private javax.sound.sampled.AudioFormat[] getOutputFormats(javax.sound.sampled.AudioFormat)
    {
        javax.sound.sampled.AudioFormat r1, $r3, $r4, $r7, $r9, $r11, $r13, $r15, $r17, $r19, $r21, $r23, $r25, $r27, $r29, $r35, $r37;
        java.util.Vector $r0;
        javax.sound.sampled.AudioFormat[] $r47;
        com.sun.media.sound.PCMtoPCMCodec r40;
        int $i0, $i1, $i2, $i3, $i4, $i5, $i6, $i7, $i8, $i9, $i10, $i11, $i12, $i13, $i14, $i15, $i16, $i17, $i18, $i19, $i20, $i21, $i22, $i23, $i24, $i25, $i26, $i27, $i28, $i29, $i30, $i31, $i32, $i33, $i34, $i35, $i36, $i37, $i38, $i39, $i40, $i41, $i42, $i43, i44, $i45;
        java.lang.Object $r2;
        java.lang.Throwable $r39;
        javax.sound.sampled.AudioFormat$Encoding $r5, $r6, $r8, $r10, $r12, $r14, $r16, $r18, $r20, $r22, $r24, $r26, $r28, $r30, $r31, $r32, $r33, $r34, $r36, $r38, $r41, $r42, $r43, $r44, $r45, $r46;
        boolean $z0, $z1, $z2, $z3, $z4, $z5, $z6;
        float $f0, $f1, $f2, $f3, $f4, $f5, $f6, $f7, $f8, $f9, $f10, $f11, $f12, $f13, $f14, $f15, $f16, $f17, $f18, $f19, $f20, $f21, $f22, $f23, $f24, $f25, $f26, $f27;

        r40 := @this: com.sun.media.sound.PCMtoPCMCodec;

        r1 := @parameter0: javax.sound.sampled.AudioFormat;

        $r0 = new java.util.Vector;

        specialinvoke $r0.<java.util.Vector: void <init>()>();

        $i0 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getSampleSizeInBits()>();

        $z0 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: boolean isBigEndian()>();

        if $i0 != 8 goto label02;

        $r31 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $r32 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: javax.sound.sampled.AudioFormat$Encoding getEncoding()>();

        $z2 = virtualinvoke $r31.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>($r32);

        if $z2 == 0 goto label01;

        $r37 = new javax.sound.sampled.AudioFormat;

        $r38 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_UNSIGNED>;

        $f26 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        $i40 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getSampleSizeInBits()>();

        $i41 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i42 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getFrameSize()>();

        $f27 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getFrameRate()>();

        specialinvoke $r37.<javax.sound.sampled.AudioFormat: void <init>(javax.sound.sampled.AudioFormat$Encoding,float,int,int,int,float,boolean)>($r38, $f26, $i40, $i41, $i42, $f27, 0);

        virtualinvoke $r0.<java.util.Vector: void addElement(java.lang.Object)>($r37);

     label01:
        $r33 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_UNSIGNED>;

        $r34 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: javax.sound.sampled.AudioFormat$Encoding getEncoding()>();

        $z3 = virtualinvoke $r33.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>($r34);

        if $z3 == 0 goto label06;

        $r35 = new javax.sound.sampled.AudioFormat;

        $r36 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $f24 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        $i37 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getSampleSizeInBits()>();

        $i38 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i39 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getFrameSize()>();

        $f25 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getFrameRate()>();

        specialinvoke $r35.<javax.sound.sampled.AudioFormat: void <init>(javax.sound.sampled.AudioFormat$Encoding,float,int,int,int,float,boolean)>($r36, $f24, $i37, $i38, $i39, $f25, 0);

        virtualinvoke $r0.<java.util.Vector: void addElement(java.lang.Object)>($r35);

        goto label06;

     label02:
        if $i0 != 16 goto label06;

        $r5 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $r6 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: javax.sound.sampled.AudioFormat$Encoding getEncoding()>();

        $z1 = virtualinvoke $r5.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>($r6);

        if $z1 == 0 goto label03;

        if $z0 == 0 goto label03;

        $r25 = new javax.sound.sampled.AudioFormat;

        $r26 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_UNSIGNED>;

        $f18 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        $i28 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getSampleSizeInBits()>();

        $i29 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i30 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getFrameSize()>();

        $f19 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getFrameRate()>();

        specialinvoke $r25.<javax.sound.sampled.AudioFormat: void <init>(javax.sound.sampled.AudioFormat$Encoding,float,int,int,int,float,boolean)>($r26, $f18, $i28, $i29, $i30, $f19, 1);

        virtualinvoke $r0.<java.util.Vector: void addElement(java.lang.Object)>($r25);

        $r27 = new javax.sound.sampled.AudioFormat;

        $r28 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $f20 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        $i31 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getSampleSizeInBits()>();

        $i32 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i33 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getFrameSize()>();

        $f21 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getFrameRate()>();

        specialinvoke $r27.<javax.sound.sampled.AudioFormat: void <init>(javax.sound.sampled.AudioFormat$Encoding,float,int,int,int,float,boolean)>($r28, $f20, $i31, $i32, $i33, $f21, 0);

        virtualinvoke $r0.<java.util.Vector: void addElement(java.lang.Object)>($r27);

        $r29 = new javax.sound.sampled.AudioFormat;

        $r30 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_UNSIGNED>;

        $f22 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        $i34 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getSampleSizeInBits()>();

        $i35 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i36 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getFrameSize()>();

        $f23 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getFrameRate()>();

        specialinvoke $r29.<javax.sound.sampled.AudioFormat: void <init>(javax.sound.sampled.AudioFormat$Encoding,float,int,int,int,float,boolean)>($r30, $f22, $i34, $i35, $i36, $f23, 0);

        virtualinvoke $r0.<java.util.Vector: void addElement(java.lang.Object)>($r29);

     label03:
        $r41 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_UNSIGNED>;

        $r42 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: javax.sound.sampled.AudioFormat$Encoding getEncoding()>();

        $z4 = virtualinvoke $r41.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>($r42);

        if $z4 == 0 goto label04;

        if $z0 == 0 goto label04;

        $r19 = new javax.sound.sampled.AudioFormat;

        $r20 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $f12 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        $i19 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getSampleSizeInBits()>();

        $i20 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i21 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getFrameSize()>();

        $f13 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getFrameRate()>();

        specialinvoke $r19.<javax.sound.sampled.AudioFormat: void <init>(javax.sound.sampled.AudioFormat$Encoding,float,int,int,int,float,boolean)>($r20, $f12, $i19, $i20, $i21, $f13, 1);

        virtualinvoke $r0.<java.util.Vector: void addElement(java.lang.Object)>($r19);

        $r21 = new javax.sound.sampled.AudioFormat;

        $r22 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_UNSIGNED>;

        $f14 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        $i22 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getSampleSizeInBits()>();

        $i23 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i24 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getFrameSize()>();

        $f15 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getFrameRate()>();

        specialinvoke $r21.<javax.sound.sampled.AudioFormat: void <init>(javax.sound.sampled.AudioFormat$Encoding,float,int,int,int,float,boolean)>($r22, $f14, $i22, $i23, $i24, $f15, 0);

        virtualinvoke $r0.<java.util.Vector: void addElement(java.lang.Object)>($r21);

        $r23 = new javax.sound.sampled.AudioFormat;

        $r24 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $f16 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        $i25 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getSampleSizeInBits()>();

        $i26 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i27 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getFrameSize()>();

        $f17 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getFrameRate()>();

        specialinvoke $r23.<javax.sound.sampled.AudioFormat: void <init>(javax.sound.sampled.AudioFormat$Encoding,float,int,int,int,float,boolean)>($r24, $f16, $i25, $i26, $i27, $f17, 0);

        virtualinvoke $r0.<java.util.Vector: void addElement(java.lang.Object)>($r23);

     label04:
        $r43 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $r44 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: javax.sound.sampled.AudioFormat$Encoding getEncoding()>();

        $z5 = virtualinvoke $r43.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>($r44);

        if $z5 == 0 goto label05;

        if $z0 != 0 goto label05;

        $r13 = new javax.sound.sampled.AudioFormat;

        $r14 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_UNSIGNED>;

        $f6 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        $i10 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getSampleSizeInBits()>();

        $i11 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i12 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getFrameSize()>();

        $f7 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getFrameRate()>();

        specialinvoke $r13.<javax.sound.sampled.AudioFormat: void <init>(javax.sound.sampled.AudioFormat$Encoding,float,int,int,int,float,boolean)>($r14, $f6, $i10, $i11, $i12, $f7, 0);

        virtualinvoke $r0.<java.util.Vector: void addElement(java.lang.Object)>($r13);

        $r15 = new javax.sound.sampled.AudioFormat;

        $r16 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $f8 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        $i13 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getSampleSizeInBits()>();

        $i14 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i15 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getFrameSize()>();

        $f9 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getFrameRate()>();

        specialinvoke $r15.<javax.sound.sampled.AudioFormat: void <init>(javax.sound.sampled.AudioFormat$Encoding,float,int,int,int,float,boolean)>($r16, $f8, $i13, $i14, $i15, $f9, 1);

        virtualinvoke $r0.<java.util.Vector: void addElement(java.lang.Object)>($r15);

        $r17 = new javax.sound.sampled.AudioFormat;

        $r18 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_UNSIGNED>;

        $f10 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        $i16 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getSampleSizeInBits()>();

        $i17 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i18 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getFrameSize()>();

        $f11 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getFrameRate()>();

        specialinvoke $r17.<javax.sound.sampled.AudioFormat: void <init>(javax.sound.sampled.AudioFormat$Encoding,float,int,int,int,float,boolean)>($r18, $f10, $i16, $i17, $i18, $f11, 1);

        virtualinvoke $r0.<java.util.Vector: void addElement(java.lang.Object)>($r17);

     label05:
        $r45 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_UNSIGNED>;

        $r46 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: javax.sound.sampled.AudioFormat$Encoding getEncoding()>();

        $z6 = virtualinvoke $r45.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>($r46);

        if $z6 == 0 goto label06;

        if $z0 != 0 goto label06;

        $r7 = new javax.sound.sampled.AudioFormat;

        $r8 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $f0 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        $i1 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getSampleSizeInBits()>();

        $i2 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i3 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getFrameSize()>();

        $f1 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getFrameRate()>();

        specialinvoke $r7.<javax.sound.sampled.AudioFormat: void <init>(javax.sound.sampled.AudioFormat$Encoding,float,int,int,int,float,boolean)>($r8, $f0, $i1, $i2, $i3, $f1, 0);

        virtualinvoke $r0.<java.util.Vector: void addElement(java.lang.Object)>($r7);

        $r9 = new javax.sound.sampled.AudioFormat;

        $r10 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_UNSIGNED>;

        $f2 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        $i4 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getSampleSizeInBits()>();

        $i5 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i6 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getFrameSize()>();

        $f3 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getFrameRate()>();

        specialinvoke $r9.<javax.sound.sampled.AudioFormat: void <init>(javax.sound.sampled.AudioFormat$Encoding,float,int,int,int,float,boolean)>($r10, $f2, $i4, $i5, $i6, $f3, 1);

        virtualinvoke $r0.<java.util.Vector: void addElement(java.lang.Object)>($r9);

        $r11 = new javax.sound.sampled.AudioFormat;

        $r12 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $f4 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        $i7 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getSampleSizeInBits()>();

        $i8 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i9 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getFrameSize()>();

        $f5 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getFrameRate()>();

        specialinvoke $r11.<javax.sound.sampled.AudioFormat: void <init>(javax.sound.sampled.AudioFormat$Encoding,float,int,int,int,float,boolean)>($r12, $f4, $i7, $i8, $i9, $f5, 1);

        virtualinvoke $r0.<java.util.Vector: void addElement(java.lang.Object)>($r11);

     label06:
        entermonitor $r0;

     label07:
        $i43 = virtualinvoke $r0.<java.util.Vector: int size()>();

        $r47 = newarray (javax.sound.sampled.AudioFormat)[$i43];

        i44 = 0;

     label08:
        $i45 = lengthof $r47;

        if i44 >= $i45 goto label09;

        $r2 = virtualinvoke $r0.<java.util.Vector: java.lang.Object elementAt(int)>(i44);

        $r3 = (javax.sound.sampled.AudioFormat) $r2;

        $r4 = (javax.sound.sampled.AudioFormat) $r3;

        $r47[i44] = $r4;

        i44 = i44 + 1;

        goto label08;

     label09:
        exitmonitor $r0;

     label10:
        goto label13;

     label11:
        $r39 := @caughtexception;

        exitmonitor $r0;

     label12:
        throw $r39;

     label13:
        return $r47;

        catch java.lang.Throwable from label07 to label10 with label11;
        catch java.lang.Throwable from label11 to label12 with label11;
    }

    static void <clinit>()
    {
        javax.sound.sampled.AudioFormat$Encoding[] $r0, $r3;
        javax.sound.sampled.AudioFormat$Encoding $r1, $r2, $r4, $r5;

        <com.sun.media.sound.PCMtoPCMCodec: int tempBufferSize> = 64;

        $r0 = newarray (javax.sound.sampled.AudioFormat$Encoding)[2];

        $r1 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $r0[0] = $r1;

        $r2 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_UNSIGNED>;

        $r0[1] = $r2;

        <com.sun.media.sound.PCMtoPCMCodec: javax.sound.sampled.AudioFormat$Encoding[] inputEncodings> = $r0;

        $r3 = newarray (javax.sound.sampled.AudioFormat$Encoding)[2];

        $r4 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $r3[0] = $r4;

        $r5 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_UNSIGNED>;

        $r3[1] = $r5;

        <com.sun.media.sound.PCMtoPCMCodec: javax.sound.sampled.AudioFormat$Encoding[] outputEncodings> = $r3;

        return;
    }
}
