public class sun.reflect.generics.repository.ClassRepository extends sun.reflect.generics.repository.GenericDeclRepository
{
    public static final sun.reflect.generics.repository.ClassRepository NONE;
    private volatile java.lang.reflect.Type superclass;
    private volatile java.lang.reflect.Type[] superInterfaces;

    private void <init>(java.lang.String, sun.reflect.generics.factory.GenericsFactory)
    {
        java.lang.String r1;
        sun.reflect.generics.repository.ClassRepository r0;
        sun.reflect.generics.factory.GenericsFactory r2;

        r0 := @this: sun.reflect.generics.repository.ClassRepository;

        r1 := @parameter0: java.lang.String;

        r2 := @parameter1: sun.reflect.generics.factory.GenericsFactory;

        specialinvoke r0.<sun.reflect.generics.repository.GenericDeclRepository: void <init>(java.lang.String,sun.reflect.generics.factory.GenericsFactory)>(r1, r2);

        return;
    }

    protected sun.reflect.generics.tree.ClassSignature parse(java.lang.String)
    {
        sun.reflect.generics.repository.ClassRepository r3;
        sun.reflect.generics.parser.SignatureParser $r0;
        java.lang.String r1;
        sun.reflect.generics.tree.ClassSignature $r2;

        r3 := @this: sun.reflect.generics.repository.ClassRepository;

        r1 := @parameter0: java.lang.String;

        $r0 = staticinvoke <sun.reflect.generics.parser.SignatureParser: sun.reflect.generics.parser.SignatureParser make()>();

        $r2 = virtualinvoke $r0.<sun.reflect.generics.parser.SignatureParser: sun.reflect.generics.tree.ClassSignature parseClassSig(java.lang.String)>(r1);

        return $r2;
    }

    public static sun.reflect.generics.repository.ClassRepository make(java.lang.String, sun.reflect.generics.factory.GenericsFactory)
    {
        java.lang.String r1;
        sun.reflect.generics.repository.ClassRepository $r0;
        sun.reflect.generics.factory.GenericsFactory r2;

        r1 := @parameter0: java.lang.String;

        r2 := @parameter1: sun.reflect.generics.factory.GenericsFactory;

        $r0 = new sun.reflect.generics.repository.ClassRepository;

        specialinvoke $r0.<sun.reflect.generics.repository.ClassRepository: void <init>(java.lang.String,sun.reflect.generics.factory.GenericsFactory)>(r1, r2);

        return $r0;
    }

    public java.lang.reflect.Type getSuperclass()
    {
        sun.reflect.generics.repository.ClassRepository r0;
        java.lang.reflect.Type $r5, r6;
        sun.reflect.generics.tree.ClassTypeSignature $r4;
        sun.reflect.generics.visitor.Reifier $r1;
        sun.reflect.generics.tree.Tree $r2;
        sun.reflect.generics.tree.ClassSignature $r3;

        r0 := @this: sun.reflect.generics.repository.ClassRepository;

        r6 = r0.<sun.reflect.generics.repository.ClassRepository: java.lang.reflect.Type superclass>;

        if r6 != null goto label1;

        $r1 = virtualinvoke r0.<sun.reflect.generics.repository.ClassRepository: sun.reflect.generics.visitor.Reifier getReifier()>();

        $r2 = virtualinvoke r0.<sun.reflect.generics.repository.ClassRepository: sun.reflect.generics.tree.Tree getTree()>();

        $r3 = (sun.reflect.generics.tree.ClassSignature) $r2;

        $r4 = virtualinvoke $r3.<sun.reflect.generics.tree.ClassSignature: sun.reflect.generics.tree.ClassTypeSignature getSuperclass()>();

        virtualinvoke $r4.<sun.reflect.generics.tree.ClassTypeSignature: void accept(sun.reflect.generics.visitor.TypeTreeVisitor)>($r1);

        $r5 = virtualinvoke $r1.<sun.reflect.generics.visitor.Reifier: java.lang.reflect.Type getResult()>();

        r6 = $r5;

        r0.<sun.reflect.generics.repository.ClassRepository: java.lang.reflect.Type superclass> = $r5;

     label1:
        return r6;
    }

    public java.lang.reflect.Type[] getSuperInterfaces()
    {
        sun.reflect.generics.tree.ClassTypeSignature $r7;
        sun.reflect.generics.repository.ClassRepository r0;
        sun.reflect.generics.tree.ClassTypeSignature[] $r5;
        java.lang.reflect.Type $r8;
        java.lang.reflect.Type[] $r2, r9;
        int $i0, $i1, i2;
        java.lang.Object $r1;
        sun.reflect.generics.visitor.Reifier $r6;
        sun.reflect.generics.tree.Tree $r3;
        sun.reflect.generics.tree.ClassSignature $r4;

        r0 := @this: sun.reflect.generics.repository.ClassRepository;

        r9 = r0.<sun.reflect.generics.repository.ClassRepository: java.lang.reflect.Type[] superInterfaces>;

        if r9 != null goto label3;

        $r3 = virtualinvoke r0.<sun.reflect.generics.repository.ClassRepository: sun.reflect.generics.tree.Tree getTree()>();

        $r4 = (sun.reflect.generics.tree.ClassSignature) $r3;

        $r5 = virtualinvoke $r4.<sun.reflect.generics.tree.ClassSignature: sun.reflect.generics.tree.ClassTypeSignature[] getSuperInterfaces()>();

        $i0 = lengthof $r5;

        r9 = newarray (java.lang.reflect.Type)[$i0];

        i2 = 0;

     label1:
        $i1 = lengthof $r5;

        if i2 >= $i1 goto label2;

        $r6 = virtualinvoke r0.<sun.reflect.generics.repository.ClassRepository: sun.reflect.generics.visitor.Reifier getReifier()>();

        $r7 = $r5[i2];

        interfaceinvoke $r7.<sun.reflect.generics.tree.TypeTree: void accept(sun.reflect.generics.visitor.TypeTreeVisitor)>($r6);

        $r8 = virtualinvoke $r6.<sun.reflect.generics.visitor.Reifier: java.lang.reflect.Type getResult()>();

        r9[i2] = $r8;

        i2 = i2 + 1;

        goto label1;

     label2:
        r0.<sun.reflect.generics.repository.ClassRepository: java.lang.reflect.Type[] superInterfaces> = r9;

     label3:
        $r1 = virtualinvoke r9.<java.lang.Object: java.lang.Object clone()>();

        $r2 = (java.lang.reflect.Type[]) $r1;

        return $r2;
    }

    protected volatile sun.reflect.generics.tree.Tree parse(java.lang.String)
    {
        java.lang.String r1;
        sun.reflect.generics.repository.ClassRepository r0;
        sun.reflect.generics.tree.ClassSignature $r2;

        r0 := @this: sun.reflect.generics.repository.ClassRepository;

        r1 := @parameter0: java.lang.String;

        $r2 = virtualinvoke r0.<sun.reflect.generics.repository.ClassRepository: sun.reflect.generics.tree.ClassSignature parse(java.lang.String)>(r1);

        return $r2;
    }

    static void <clinit>()
    {
        sun.reflect.generics.repository.ClassRepository $r0;

        $r0 = staticinvoke <sun.reflect.generics.repository.ClassRepository: sun.reflect.generics.repository.ClassRepository make(java.lang.String,sun.reflect.generics.factory.GenericsFactory)>("Ljava/lang/Object;", null);

        <sun.reflect.generics.repository.ClassRepository: sun.reflect.generics.repository.ClassRepository NONE> = $r0;

        return;
    }
}
