public final class sun.audio.AudioData extends java.lang.Object
{
    private static final javax.sound.sampled.AudioFormat DEFAULT_FORMAT;
    javax.sound.sampled.AudioFormat format;
    byte[] buffer;

    public void <init>(byte[])
    {
        byte[] r1;
        javax.sound.sampled.UnsupportedAudioFileException $r6;
        javax.sound.sampled.AudioFormat $r2, $r5;
        java.io.IOException $r7;
        java.io.ByteArrayInputStream $r3;
        sun.audio.AudioData r0;
        javax.sound.sampled.AudioInputStream $r4;

        r0 := @this: sun.audio.AudioData;

        r1 := @parameter0: byte[];

        $r2 = <sun.audio.AudioData: javax.sound.sampled.AudioFormat DEFAULT_FORMAT>;

        specialinvoke r0.<sun.audio.AudioData: void <init>(javax.sound.sampled.AudioFormat,byte[])>($r2, r1);

     label1:
        $r3 = new java.io.ByteArrayInputStream;

        specialinvoke $r3.<java.io.ByteArrayInputStream: void <init>(byte[])>(r1);

        $r4 = staticinvoke <javax.sound.sampled.AudioSystem: javax.sound.sampled.AudioInputStream getAudioInputStream(java.io.InputStream)>($r3);

        $r5 = virtualinvoke $r4.<javax.sound.sampled.AudioInputStream: javax.sound.sampled.AudioFormat getFormat()>();

        r0.<sun.audio.AudioData: javax.sound.sampled.AudioFormat format> = $r5;

        virtualinvoke $r4.<javax.sound.sampled.AudioInputStream: void close()>();

     label2:
        goto label5;

     label3:
        $r7 := @caughtexception;

        goto label5;

     label4:
        $r6 := @caughtexception;

     label5:
        return;

        catch java.io.IOException from label1 to label2 with label3;
        catch javax.sound.sampled.UnsupportedAudioFileException from label1 to label2 with label4;
    }

    void <init>(javax.sound.sampled.AudioFormat, byte[])
    {
        byte[] r2, $r3;
        sun.audio.AudioData r0;
        int $i0;
        javax.sound.sampled.AudioFormat r1;

        r0 := @this: sun.audio.AudioData;

        r1 := @parameter0: javax.sound.sampled.AudioFormat;

        r2 := @parameter1: byte[];

        specialinvoke r0.<java.lang.Object: void <init>()>();

        r0.<sun.audio.AudioData: javax.sound.sampled.AudioFormat format> = r1;

        if r2 == null goto label1;

        $i0 = lengthof r2;

        $r3 = staticinvoke <java.util.Arrays: byte[] copyOf(byte[],int)>(r2, $i0);

        r0.<sun.audio.AudioData: byte[] buffer> = $r3;

     label1:
        return;
    }

    static void <clinit>()
    {
        javax.sound.sampled.AudioFormat $r0;
        javax.sound.sampled.AudioFormat$Encoding $r1;

        $r0 = new javax.sound.sampled.AudioFormat;

        $r1 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding ULAW>;

        specialinvoke $r0.<javax.sound.sampled.AudioFormat: void <init>(javax.sound.sampled.AudioFormat$Encoding,float,int,int,int,float,boolean)>($r1, 8000.0F, 8, 1, 1, 8000.0F, 1);

        <sun.audio.AudioData: javax.sound.sampled.AudioFormat DEFAULT_FORMAT> = $r0;

        return;
    }
}
