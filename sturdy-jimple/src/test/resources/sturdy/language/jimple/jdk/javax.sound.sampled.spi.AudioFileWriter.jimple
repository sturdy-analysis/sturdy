public abstract class javax.sound.sampled.spi.AudioFileWriter extends java.lang.Object
{

    public void <init>()
    {
        javax.sound.sampled.spi.AudioFileWriter r0;

        r0 := @this: javax.sound.sampled.spi.AudioFileWriter;

        specialinvoke r0.<java.lang.Object: void <init>()>();

        return;
    }

    public abstract javax.sound.sampled.AudioFileFormat$Type[] getAudioFileTypes();

    public boolean isFileTypeSupported(javax.sound.sampled.AudioFileFormat$Type)
    {
        int $i0, i1;
        javax.sound.sampled.AudioFileFormat$Type[] $r1;
        javax.sound.sampled.AudioFileFormat$Type r2, $r3;
        javax.sound.sampled.spi.AudioFileWriter r0;
        boolean $z0;

        r0 := @this: javax.sound.sampled.spi.AudioFileWriter;

        r2 := @parameter0: javax.sound.sampled.AudioFileFormat$Type;

        $r1 = virtualinvoke r0.<javax.sound.sampled.spi.AudioFileWriter: javax.sound.sampled.AudioFileFormat$Type[] getAudioFileTypes()>();

        i1 = 0;

     label1:
        $i0 = lengthof $r1;

        if i1 >= $i0 goto label3;

        $r3 = $r1[i1];

        $z0 = virtualinvoke r2.<javax.sound.sampled.AudioFileFormat$Type: boolean equals(java.lang.Object)>($r3);

        if $z0 == 0 goto label2;

        return 1;

     label2:
        i1 = i1 + 1;

        goto label1;

     label3:
        return 0;
    }

    public abstract javax.sound.sampled.AudioFileFormat$Type[] getAudioFileTypes(javax.sound.sampled.AudioInputStream);

    public boolean isFileTypeSupported(javax.sound.sampled.AudioFileFormat$Type, javax.sound.sampled.AudioInputStream)
    {
        javax.sound.sampled.AudioFileFormat$Type r3, $r4;
        javax.sound.sampled.spi.AudioFileWriter r0;
        javax.sound.sampled.AudioInputStream r1;
        int $i0, i1;
        javax.sound.sampled.AudioFileFormat$Type[] $r2;
        boolean $z0;

        r0 := @this: javax.sound.sampled.spi.AudioFileWriter;

        r3 := @parameter0: javax.sound.sampled.AudioFileFormat$Type;

        r1 := @parameter1: javax.sound.sampled.AudioInputStream;

        $r2 = virtualinvoke r0.<javax.sound.sampled.spi.AudioFileWriter: javax.sound.sampled.AudioFileFormat$Type[] getAudioFileTypes(javax.sound.sampled.AudioInputStream)>(r1);

        i1 = 0;

     label1:
        $i0 = lengthof $r2;

        if i1 >= $i0 goto label3;

        $r4 = $r2[i1];

        $z0 = virtualinvoke r3.<javax.sound.sampled.AudioFileFormat$Type: boolean equals(java.lang.Object)>($r4);

        if $z0 == 0 goto label2;

        return 1;

     label2:
        i1 = i1 + 1;

        goto label1;

     label3:
        return 0;
    }

    public abstract int write(javax.sound.sampled.AudioInputStream, javax.sound.sampled.AudioFileFormat$Type, java.io.OutputStream) throws java.io.IOException;

    public abstract int write(javax.sound.sampled.AudioInputStream, javax.sound.sampled.AudioFileFormat$Type, java.io.File) throws java.io.IOException;
}
