public final class com.sun.media.sound.AlawCodec extends com.sun.media.sound.SunCodec
{
    private static final byte[] ALAW_TABH;
    private static final byte[] ALAW_TABL;
    private static final javax.sound.sampled.AudioFormat$Encoding[] alawEncodings;
    private static final short[] seg_end;

    public void <init>()
    {
        com.sun.media.sound.AlawCodec r0;
        javax.sound.sampled.AudioFormat$Encoding[] $r1, $r2;

        r0 := @this: com.sun.media.sound.AlawCodec;

        $r2 = <com.sun.media.sound.AlawCodec: javax.sound.sampled.AudioFormat$Encoding[] alawEncodings>;

        $r1 = <com.sun.media.sound.AlawCodec: javax.sound.sampled.AudioFormat$Encoding[] alawEncodings>;

        specialinvoke r0.<com.sun.media.sound.SunCodec: void <init>(javax.sound.sampled.AudioFormat$Encoding[],javax.sound.sampled.AudioFormat$Encoding[])>($r2, $r1);

        return;
    }

    public javax.sound.sampled.AudioFormat$Encoding[] getTargetEncodings(javax.sound.sampled.AudioFormat)
    {
        javax.sound.sampled.AudioFormat r0;
        com.sun.media.sound.AlawCodec r10;
        int $i0, $i1;
        boolean $z0, $z1;
        javax.sound.sampled.AudioFormat$Encoding $r1, $r2, $r3, $r4, $r7, $r9;
        javax.sound.sampled.AudioFormat$Encoding[] $r5, $r6, $r8, r11, r12;

        r10 := @this: com.sun.media.sound.AlawCodec;

        r0 := @parameter0: javax.sound.sampled.AudioFormat;

        $r1 = virtualinvoke r0.<javax.sound.sampled.AudioFormat: javax.sound.sampled.AudioFormat$Encoding getEncoding()>();

        $r2 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $z0 = virtualinvoke $r1.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>($r2);

        if $z0 == 0 goto label2;

        $i1 = virtualinvoke r0.<javax.sound.sampled.AudioFormat: int getSampleSizeInBits()>();

        if $i1 != 16 goto label1;

        r11 = newarray (javax.sound.sampled.AudioFormat$Encoding)[1];

        $r9 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding ALAW>;

        r11[0] = $r9;

        return r11;

     label1:
        $r8 = newarray (javax.sound.sampled.AudioFormat$Encoding)[0];

        return $r8;

     label2:
        $r3 = virtualinvoke r0.<javax.sound.sampled.AudioFormat: javax.sound.sampled.AudioFormat$Encoding getEncoding()>();

        $r4 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding ALAW>;

        $z1 = virtualinvoke $r3.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>($r4);

        if $z1 == 0 goto label4;

        $i0 = virtualinvoke r0.<javax.sound.sampled.AudioFormat: int getSampleSizeInBits()>();

        if $i0 != 8 goto label3;

        r12 = newarray (javax.sound.sampled.AudioFormat$Encoding)[1];

        $r7 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        r12[0] = $r7;

        return r12;

     label3:
        $r6 = newarray (javax.sound.sampled.AudioFormat$Encoding)[0];

        return $r6;

     label4:
        $r5 = newarray (javax.sound.sampled.AudioFormat$Encoding)[0];

        return $r5;
    }

    public javax.sound.sampled.AudioFormat[] getTargetFormats(javax.sound.sampled.AudioFormat$Encoding, javax.sound.sampled.AudioFormat)
    {
        javax.sound.sampled.AudioFormat r4;
        com.sun.media.sound.AlawCodec r7;
        javax.sound.sampled.AudioFormat$Encoding r0, $r1, $r2, $r5, $r6, $r9, $r10;
        javax.sound.sampled.AudioFormat[] $r3, $r8;
        boolean $z0, $z1, $z2, $z3;

        r7 := @this: com.sun.media.sound.AlawCodec;

        r0 := @parameter0: javax.sound.sampled.AudioFormat$Encoding;

        r4 := @parameter1: javax.sound.sampled.AudioFormat;

        $r1 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $z0 = virtualinvoke r0.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>($r1);

        if $z0 == 0 goto label1;

        $r9 = virtualinvoke r4.<javax.sound.sampled.AudioFormat: javax.sound.sampled.AudioFormat$Encoding getEncoding()>();

        $r10 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding ALAW>;

        $z3 = virtualinvoke $r9.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>($r10);

        if $z3 != 0 goto label2;

     label1:
        $r2 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding ALAW>;

        $z1 = virtualinvoke r0.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>($r2);

        if $z1 == 0 goto label3;

        $r5 = virtualinvoke r4.<javax.sound.sampled.AudioFormat: javax.sound.sampled.AudioFormat$Encoding getEncoding()>();

        $r6 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $z2 = virtualinvoke $r5.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>($r6);

        if $z2 == 0 goto label3;

     label2:
        $r8 = specialinvoke r7.<com.sun.media.sound.AlawCodec: javax.sound.sampled.AudioFormat[] getOutputFormats(javax.sound.sampled.AudioFormat)>(r4);

        return $r8;

     label3:
        $r3 = newarray (javax.sound.sampled.AudioFormat)[0];

        return $r3;
    }

    public javax.sound.sampled.AudioInputStream getAudioInputStream(javax.sound.sampled.AudioFormat$Encoding, javax.sound.sampled.AudioInputStream)
    {
        javax.sound.sampled.AudioFormat $r1, $r5, $r11, $r19, $r22, $r26, r33;
        com.sun.media.sound.AlawCodec r4;
        javax.sound.sampled.AudioInputStream r0, $r20;
        boolean $z0, $z1, $z2, $z3, $z4, $z5, $z6;
        float $f0, $f1, $f2, $f3;
        java.lang.StringBuilder $r9, $r10, $r13, $r14, $r16, $r24, $r25, $r28, $r29, $r31;
        int $i0, $i1, $i2, $i3, $i4;
        java.lang.String $r12, $r15, $r17, $r27, $r30, $r32;
        java.lang.IllegalArgumentException $r8, $r23;
        javax.sound.sampled.AudioFormat$Encoding $r2, r3, $r6, $r7, $r18, $r21;

        r4 := @this: com.sun.media.sound.AlawCodec;

        r3 := @parameter0: javax.sound.sampled.AudioFormat$Encoding;

        r0 := @parameter1: javax.sound.sampled.AudioInputStream;

        $r1 = virtualinvoke r0.<javax.sound.sampled.AudioInputStream: javax.sound.sampled.AudioFormat getFormat()>();

        $r2 = virtualinvoke $r1.<javax.sound.sampled.AudioFormat: javax.sound.sampled.AudioFormat$Encoding getEncoding()>();

        $z0 = virtualinvoke $r2.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>(r3);

        if $z0 == 0 goto label1;

        return r0;

     label1:
        $r5 = virtualinvoke r0.<javax.sound.sampled.AudioInputStream: javax.sound.sampled.AudioFormat getFormat()>();

        $z1 = virtualinvoke r4.<com.sun.media.sound.AlawCodec: boolean isConversionSupported(javax.sound.sampled.AudioFormat$Encoding,javax.sound.sampled.AudioFormat)>(r3, $r5);

        if $z1 != 0 goto label2;

        $r23 = new java.lang.IllegalArgumentException;

        $r24 = new java.lang.StringBuilder;

        specialinvoke $r24.<java.lang.StringBuilder: void <init>()>();

        $r25 = virtualinvoke $r24.<java.lang.StringBuilder: java.lang.StringBuilder append(java.lang.String)>("Unsupported conversion: ");

        $r26 = virtualinvoke r0.<javax.sound.sampled.AudioInputStream: javax.sound.sampled.AudioFormat getFormat()>();

        $r27 = virtualinvoke $r26.<javax.sound.sampled.AudioFormat: java.lang.String toString()>();

        $r28 = virtualinvoke $r25.<java.lang.StringBuilder: java.lang.StringBuilder append(java.lang.String)>($r27);

        $r29 = virtualinvoke $r28.<java.lang.StringBuilder: java.lang.StringBuilder append(java.lang.String)>(" to ");

        $r30 = virtualinvoke r3.<javax.sound.sampled.AudioFormat$Encoding: java.lang.String toString()>();

        $r31 = virtualinvoke $r29.<java.lang.StringBuilder: java.lang.StringBuilder append(java.lang.String)>($r30);

        $r32 = virtualinvoke $r31.<java.lang.StringBuilder: java.lang.String toString()>();

        specialinvoke $r23.<java.lang.IllegalArgumentException: void <init>(java.lang.String)>($r32);

        throw $r23;

     label2:
        $r6 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding ALAW>;

        $z2 = virtualinvoke $r2.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>($r6);

        if $z2 == 0 goto label3;

        $r21 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $z5 = virtualinvoke r3.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>($r21);

        if $z5 == 0 goto label3;

        $r22 = new javax.sound.sampled.AudioFormat;

        $f2 = virtualinvoke $r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        $i2 = virtualinvoke $r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i3 = virtualinvoke $r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i4 = 2 * $i3;

        $f3 = virtualinvoke $r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        $z6 = virtualinvoke $r1.<javax.sound.sampled.AudioFormat: boolean isBigEndian()>();

        specialinvoke $r22.<javax.sound.sampled.AudioFormat: void <init>(javax.sound.sampled.AudioFormat$Encoding,float,int,int,int,float,boolean)>(r3, $f2, 16, $i2, $i4, $f3, $z6);

        r33 = $r22;

        goto label5;

     label3:
        $r7 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $z3 = virtualinvoke $r2.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>($r7);

        if $z3 == 0 goto label4;

        $r18 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding ALAW>;

        $z4 = virtualinvoke r3.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>($r18);

        if $z4 == 0 goto label4;

        $r19 = new javax.sound.sampled.AudioFormat;

        $f0 = virtualinvoke $r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        $i0 = virtualinvoke $r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i1 = virtualinvoke $r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $f1 = virtualinvoke $r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        specialinvoke $r19.<javax.sound.sampled.AudioFormat: void <init>(javax.sound.sampled.AudioFormat$Encoding,float,int,int,int,float,boolean)>(r3, $f0, 8, $i0, $i1, $f1, 0);

        r33 = $r19;

        goto label5;

     label4:
        $r8 = new java.lang.IllegalArgumentException;

        $r9 = new java.lang.StringBuilder;

        specialinvoke $r9.<java.lang.StringBuilder: void <init>()>();

        $r10 = virtualinvoke $r9.<java.lang.StringBuilder: java.lang.StringBuilder append(java.lang.String)>("Unsupported conversion: ");

        $r11 = virtualinvoke r0.<javax.sound.sampled.AudioInputStream: javax.sound.sampled.AudioFormat getFormat()>();

        $r12 = virtualinvoke $r11.<javax.sound.sampled.AudioFormat: java.lang.String toString()>();

        $r13 = virtualinvoke $r10.<java.lang.StringBuilder: java.lang.StringBuilder append(java.lang.String)>($r12);

        $r14 = virtualinvoke $r13.<java.lang.StringBuilder: java.lang.StringBuilder append(java.lang.String)>(" to ");

        $r15 = virtualinvoke r3.<javax.sound.sampled.AudioFormat$Encoding: java.lang.String toString()>();

        $r16 = virtualinvoke $r14.<java.lang.StringBuilder: java.lang.StringBuilder append(java.lang.String)>($r15);

        $r17 = virtualinvoke $r16.<java.lang.StringBuilder: java.lang.String toString()>();

        specialinvoke $r8.<java.lang.IllegalArgumentException: void <init>(java.lang.String)>($r17);

        throw $r8;

     label5:
        $r20 = virtualinvoke r4.<com.sun.media.sound.AlawCodec: javax.sound.sampled.AudioInputStream getAudioInputStream(javax.sound.sampled.AudioFormat,javax.sound.sampled.AudioInputStream)>(r33, r0);

        return $r20;
    }

    public javax.sound.sampled.AudioInputStream getAudioInputStream(javax.sound.sampled.AudioFormat, javax.sound.sampled.AudioInputStream)
    {
        javax.sound.sampled.AudioInputStream r2, $r3;
        com.sun.media.sound.AlawCodec r0;
        javax.sound.sampled.AudioFormat r1;

        r0 := @this: com.sun.media.sound.AlawCodec;

        r1 := @parameter0: javax.sound.sampled.AudioFormat;

        r2 := @parameter1: javax.sound.sampled.AudioInputStream;

        $r3 = specialinvoke r0.<com.sun.media.sound.AlawCodec: javax.sound.sampled.AudioInputStream getConvertedStream(javax.sound.sampled.AudioFormat,javax.sound.sampled.AudioInputStream)>(r1, r2);

        return $r3;
    }

    private javax.sound.sampled.AudioInputStream getConvertedStream(javax.sound.sampled.AudioFormat, javax.sound.sampled.AudioInputStream)
    {
        com.sun.media.sound.AlawCodec$AlawCodecStream $r3;
        javax.sound.sampled.AudioInputStream r0, r5;
        javax.sound.sampled.AudioFormat $r1, r2;
        com.sun.media.sound.AlawCodec r4;
        boolean $z0;

        r4 := @this: com.sun.media.sound.AlawCodec;

        r2 := @parameter0: javax.sound.sampled.AudioFormat;

        r0 := @parameter1: javax.sound.sampled.AudioInputStream;

        $r1 = virtualinvoke r0.<javax.sound.sampled.AudioInputStream: javax.sound.sampled.AudioFormat getFormat()>();

        $z0 = virtualinvoke $r1.<javax.sound.sampled.AudioFormat: boolean matches(javax.sound.sampled.AudioFormat)>(r2);

        if $z0 == 0 goto label1;

        r5 = r0;

        goto label2;

     label1:
        $r3 = new com.sun.media.sound.AlawCodec$AlawCodecStream;

        specialinvoke $r3.<com.sun.media.sound.AlawCodec$AlawCodecStream: void <init>(com.sun.media.sound.AlawCodec,javax.sound.sampled.AudioInputStream,javax.sound.sampled.AudioFormat)>(r4, r0, r2);

        r5 = $r3;

     label2:
        return r5;
    }

    private javax.sound.sampled.AudioFormat[] getOutputFormats(javax.sound.sampled.AudioFormat)
    {
        javax.sound.sampled.AudioFormat r1, $r7, $r8, $r9, $r11, $r14;
        com.sun.media.sound.AlawCodec r16;
        java.util.Vector $r0;
        boolean $z0, $z1;
        float $f0, $f1, $f2, $f3, $f4, $f5;
        javax.sound.sampled.AudioFormat[] $r13;
        int $i0, $i1, $i2, $i3, $i4, $i5, $i6, $i7, $i8, i9, $i10;
        javax.sound.sampled.AudioFormat$Encoding $r2, $r3, $r4, $r5, $r10, $r12, $r15;
        java.lang.Object $r6;

        r16 := @this: com.sun.media.sound.AlawCodec;

        r1 := @parameter0: javax.sound.sampled.AudioFormat;

        $r0 = new java.util.Vector;

        specialinvoke $r0.<java.util.Vector: void <init>()>();

        $r2 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $r3 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: javax.sound.sampled.AudioFormat$Encoding getEncoding()>();

        $z0 = virtualinvoke $r2.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>($r3);

        if $z0 == 0 goto label1;

        $r14 = new javax.sound.sampled.AudioFormat;

        $r15 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding ALAW>;

        $f4 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        $i7 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i8 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $f5 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        specialinvoke $r14.<javax.sound.sampled.AudioFormat: void <init>(javax.sound.sampled.AudioFormat$Encoding,float,int,int,int,float,boolean)>($r15, $f4, 8, $i7, $i8, $f5, 0);

        virtualinvoke $r0.<java.util.Vector: void addElement(java.lang.Object)>($r14);

     label1:
        $r4 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding ALAW>;

        $r5 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: javax.sound.sampled.AudioFormat$Encoding getEncoding()>();

        $z1 = virtualinvoke $r4.<javax.sound.sampled.AudioFormat$Encoding: boolean equals(java.lang.Object)>($r5);

        if $z1 == 0 goto label2;

        $r9 = new javax.sound.sampled.AudioFormat;

        $r10 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $f0 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        $i1 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i2 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i3 = $i2 * 2;

        $f1 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        specialinvoke $r9.<javax.sound.sampled.AudioFormat: void <init>(javax.sound.sampled.AudioFormat$Encoding,float,int,int,int,float,boolean)>($r10, $f0, 16, $i1, $i3, $f1, 0);

        virtualinvoke $r0.<java.util.Vector: void addElement(java.lang.Object)>($r9);

        $r11 = new javax.sound.sampled.AudioFormat;

        $r12 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $f2 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        $i4 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i5 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: int getChannels()>();

        $i6 = $i5 * 2;

        $f3 = virtualinvoke r1.<javax.sound.sampled.AudioFormat: float getSampleRate()>();

        specialinvoke $r11.<javax.sound.sampled.AudioFormat: void <init>(javax.sound.sampled.AudioFormat$Encoding,float,int,int,int,float,boolean)>($r12, $f2, 16, $i4, $i6, $f3, 1);

        virtualinvoke $r0.<java.util.Vector: void addElement(java.lang.Object)>($r11);

     label2:
        $i0 = virtualinvoke $r0.<java.util.Vector: int size()>();

        $r13 = newarray (javax.sound.sampled.AudioFormat)[$i0];

        i9 = 0;

     label3:
        $i10 = lengthof $r13;

        if i9 >= $i10 goto label4;

        $r6 = virtualinvoke $r0.<java.util.Vector: java.lang.Object elementAt(int)>(i9);

        $r7 = (javax.sound.sampled.AudioFormat) $r6;

        $r8 = (javax.sound.sampled.AudioFormat) $r7;

        $r13[i9] = $r8;

        i9 = i9 + 1;

        goto label3;

     label4:
        return $r13;
    }

    static byte[] access$000()
    {
        byte[] $r0;

        $r0 = <com.sun.media.sound.AlawCodec: byte[] ALAW_TABH>;

        return $r0;
    }

    static byte[] access$100()
    {
        byte[] $r0;

        $r0 = <com.sun.media.sound.AlawCodec: byte[] ALAW_TABL>;

        return $r0;
    }

    static short[] access$200()
    {
        short[] $r0;

        $r0 = <com.sun.media.sound.AlawCodec: short[] seg_end>;

        return $r0;
    }

    static void <clinit>()
    {
        byte[] $r0, $r1, $r6, $r7;
        byte $b6, $b8;
        int i0, $i1, i2, $i3, i4, $i5, $i7, $i9, i10, i11;
        javax.sound.sampled.AudioFormat$Encoding $r3, $r4;
        short[] $r5;
        javax.sound.sampled.AudioFormat$Encoding[] $r2;

        $r0 = newarray (byte)[256];

        <com.sun.media.sound.AlawCodec: byte[] ALAW_TABH> = $r0;

        $r1 = newarray (byte)[256];

        <com.sun.media.sound.AlawCodec: byte[] ALAW_TABL> = $r1;

        $r2 = newarray (javax.sound.sampled.AudioFormat$Encoding)[2];

        $r3 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding ALAW>;

        $r2[0] = $r3;

        $r4 = <javax.sound.sampled.AudioFormat$Encoding: javax.sound.sampled.AudioFormat$Encoding PCM_SIGNED>;

        $r2[1] = $r4;

        <com.sun.media.sound.AlawCodec: javax.sound.sampled.AudioFormat$Encoding[] alawEncodings> = $r2;

        $r5 = newarray (short)[8];

        $r5[0] = 255;

        $r5[1] = 511;

        $r5[2] = 1023;

        $r5[3] = 2047;

        $r5[4] = 4095;

        $r5[5] = 8191;

        $r5[6] = 16383;

        $r5[7] = 32767;

        <com.sun.media.sound.AlawCodec: short[] seg_end> = $r5;

        i10 = 0;

     label1:
        if i10 >= 256 goto label5;

        i0 = i10 ^ 85;

        $i1 = i0 & 15;

        i2 = $i1 << 4;

        $i3 = i0 & 112;

        i4 = $i3 >> 4;

        i11 = i2 + 8;

        if i4 < 1 goto label2;

        i11 = i11 + 256;

     label2:
        if i4 <= 1 goto label3;

        $i9 = i4 - 1;

        i11 = i11 << $i9;

     label3:
        $i5 = i0 & 128;

        if $i5 != 0 goto label4;

        i11 = neg i11;

     label4:
        $r6 = <com.sun.media.sound.AlawCodec: byte[] ALAW_TABL>;

        $b6 = (byte) i11;

        $r6[i10] = $b6;

        $r7 = <com.sun.media.sound.AlawCodec: byte[] ALAW_TABH>;

        $i7 = i11 >> 8;

        $b8 = (byte) $i7;

        $r7[i10] = $b8;

        i10 = i10 + 1;

        goto label1;

     label5:
        return;
    }
}
