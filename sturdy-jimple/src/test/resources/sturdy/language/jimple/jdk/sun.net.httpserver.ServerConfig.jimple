class sun.net.httpserver.ServerConfig extends java.lang.Object
{
    private static final int DEFAULT_CLOCK_TICK;
    private static final long DEFAULT_IDLE_INTERVAL;
    private static final int DEFAULT_MAX_IDLE_CONNECTIONS;
    private static final long DEFAULT_MAX_REQ_TIME;
    private static final long DEFAULT_MAX_RSP_TIME;
    private static final long DEFAULT_TIMER_MILLIS;
    private static final int DEFAULT_MAX_REQ_HEADERS;
    private static final long DEFAULT_DRAIN_AMOUNT;
    private static int clockTick;
    private static long idleInterval;
    private static long drainAmount;
    private static int maxIdleConnections;
    private static int maxReqHeaders;
    private static long maxReqTime;
    private static long maxRspTime;
    private static long timerMillis;
    private static boolean debug;
    private static boolean noDelay;

    void <init>()
    {
        sun.net.httpserver.ServerConfig r0;

        r0 := @this: sun.net.httpserver.ServerConfig;

        specialinvoke r0.<java.lang.Object: void <init>()>();

        return;
    }

    static void checkLegacyProperties(java.util.logging.Logger)
    {
        java.util.logging.Logger r1;
        sun.net.httpserver.ServerConfig$2 $r0;

        r1 := @parameter0: java.util.logging.Logger;

        $r0 = new sun.net.httpserver.ServerConfig$2;

        specialinvoke $r0.<sun.net.httpserver.ServerConfig$2: void <init>(java.util.logging.Logger)>(r1);

        staticinvoke <java.security.AccessController: java.lang.Object doPrivileged(java.security.PrivilegedAction)>($r0);

        return;
    }

    static boolean debugEnabled()
    {
        boolean $z0;

        $z0 = <sun.net.httpserver.ServerConfig: boolean debug>;

        return $z0;
    }

    static long getIdleInterval()
    {
        long $l0;

        $l0 = <sun.net.httpserver.ServerConfig: long idleInterval>;

        return $l0;
    }

    static int getClockTick()
    {
        int $i0;

        $i0 = <sun.net.httpserver.ServerConfig: int clockTick>;

        return $i0;
    }

    static int getMaxIdleConnections()
    {
        int $i0;

        $i0 = <sun.net.httpserver.ServerConfig: int maxIdleConnections>;

        return $i0;
    }

    static long getDrainAmount()
    {
        long $l0;

        $l0 = <sun.net.httpserver.ServerConfig: long drainAmount>;

        return $l0;
    }

    static int getMaxReqHeaders()
    {
        int $i0;

        $i0 = <sun.net.httpserver.ServerConfig: int maxReqHeaders>;

        return $i0;
    }

    static long getMaxReqTime()
    {
        long $l0;

        $l0 = <sun.net.httpserver.ServerConfig: long maxReqTime>;

        return $l0;
    }

    static long getMaxRspTime()
    {
        long $l0;

        $l0 = <sun.net.httpserver.ServerConfig: long maxRspTime>;

        return $l0;
    }

    static long getTimerMillis()
    {
        long $l0;

        $l0 = <sun.net.httpserver.ServerConfig: long timerMillis>;

        return $l0;
    }

    static boolean noDelay()
    {
        boolean $z0;

        $z0 = <sun.net.httpserver.ServerConfig: boolean noDelay>;

        return $z0;
    }

    static long access$002(long)
    {
        long l0;

        l0 := @parameter0: long;

        <sun.net.httpserver.ServerConfig: long idleInterval> = l0;

        return l0;
    }

    static int access$102(int)
    {
        int i0;

        i0 := @parameter0: int;

        <sun.net.httpserver.ServerConfig: int clockTick> = i0;

        return i0;
    }

    static int access$202(int)
    {
        int i0;

        i0 := @parameter0: int;

        <sun.net.httpserver.ServerConfig: int maxIdleConnections> = i0;

        return i0;
    }

    static long access$302(long)
    {
        long l0;

        l0 := @parameter0: long;

        <sun.net.httpserver.ServerConfig: long drainAmount> = l0;

        return l0;
    }

    static int access$402(int)
    {
        int i0;

        i0 := @parameter0: int;

        <sun.net.httpserver.ServerConfig: int maxReqHeaders> = i0;

        return i0;
    }

    static long access$502(long)
    {
        long l0;

        l0 := @parameter0: long;

        <sun.net.httpserver.ServerConfig: long maxReqTime> = l0;

        return l0;
    }

    static long access$602(long)
    {
        long l0;

        l0 := @parameter0: long;

        <sun.net.httpserver.ServerConfig: long maxRspTime> = l0;

        return l0;
    }

    static long access$702(long)
    {
        long l0;

        l0 := @parameter0: long;

        <sun.net.httpserver.ServerConfig: long timerMillis> = l0;

        return l0;
    }

    static boolean access$802(boolean)
    {
        boolean z0;

        z0 := @parameter0: boolean;

        <sun.net.httpserver.ServerConfig: boolean debug> = z0;

        return z0;
    }

    static boolean access$902(boolean)
    {
        boolean z0;

        z0 := @parameter0: boolean;

        <sun.net.httpserver.ServerConfig: boolean noDelay> = z0;

        return z0;
    }

    static void <clinit>()
    {
        sun.net.httpserver.ServerConfig$1 $r0;

        <sun.net.httpserver.ServerConfig: long DEFAULT_DRAIN_AMOUNT> = 65536L;

        <sun.net.httpserver.ServerConfig: int DEFAULT_MAX_REQ_HEADERS> = 200;

        <sun.net.httpserver.ServerConfig: long DEFAULT_TIMER_MILLIS> = 1000L;

        <sun.net.httpserver.ServerConfig: long DEFAULT_MAX_RSP_TIME> = -1L;

        <sun.net.httpserver.ServerConfig: long DEFAULT_MAX_REQ_TIME> = -1L;

        <sun.net.httpserver.ServerConfig: int DEFAULT_MAX_IDLE_CONNECTIONS> = 200;

        <sun.net.httpserver.ServerConfig: long DEFAULT_IDLE_INTERVAL> = 30L;

        <sun.net.httpserver.ServerConfig: int DEFAULT_CLOCK_TICK> = 10000;

        $r0 = new sun.net.httpserver.ServerConfig$1;

        specialinvoke $r0.<sun.net.httpserver.ServerConfig$1: void <init>()>();

        staticinvoke <java.security.AccessController: java.lang.Object doPrivileged(java.security.PrivilegedAction)>($r0);

        return;
    }
}
