public final class sun.tools.jar.resources.jar extends java.util.ListResourceBundle
{

    public void <init>()
    {
        sun.tools.jar.resources.jar r0;

        r0 := @this: sun.tools.jar.resources.jar;

        specialinvoke r0.<java.util.ListResourceBundle: void <init>()>();

        return;
    }

    protected final java.lang.Object[][] getContents()
    {
        java.lang.Object[] $r1, $r2, $r3, $r4, $r5, $r6, $r7, $r8, $r9, $r10, $r11, $r12, $r13, $r14, $r15, $r16, $r17, $r18, $r19, $r20, $r21, $r22;
        java.lang.Object[][] $r0;
        sun.tools.jar.resources.jar r23;

        r23 := @this: sun.tools.jar.resources.jar;

        $r0 = newarray (java.lang.Object[])[22];

        $r1 = newarray (java.lang.Object)[2];

        $r1[0] = "error.bad.cflag";

        $r1[1] = "\'c\' flag requires manifest or input files to be specified!";

        $r0[0] = $r1;

        $r2 = newarray (java.lang.Object)[2];

        $r2[0] = "error.bad.eflag";

        $r2[1] = "\'e\' flag and manifest with the \'Main-Class\' attribute cannot be specified \ntogether!";

        $r0[1] = $r2;

        $r3 = newarray (java.lang.Object)[2];

        $r3[0] = "error.bad.option";

        $r3[1] = "One of options -{ctxu} must be specified.";

        $r0[2] = $r3;

        $r4 = newarray (java.lang.Object)[2];

        $r4[0] = "error.bad.uflag";

        $r4[1] = "\'u\' flag requires manifest, \'e\' flag or input files to be specified!";

        $r0[3] = $r4;

        $r5 = newarray (java.lang.Object)[2];

        $r5[0] = "error.cant.open";

        $r5[1] = "can\'\'t open: {0} ";

        $r0[4] = $r5;

        $r6 = newarray (java.lang.Object)[2];

        $r6[0] = "error.create.dir";

        $r6[1] = "{0} : could not create directory";

        $r0[5] = $r6;

        $r7 = newarray (java.lang.Object)[2];

        $r7[0] = "error.create.tempfile";

        $r7[1] = "Could not create a temporary file";

        $r0[6] = $r7;

        $r8 = newarray (java.lang.Object)[2];

        $r8[0] = "error.illegal.option";

        $r8[1] = "Illegal option: {0}";

        $r0[7] = $r8;

        $r9 = newarray (java.lang.Object)[2];

        $r9[0] = "error.incorrect.length";

        $r9[1] = "incorrect length while processing: {0}";

        $r0[8] = $r9;

        $r10 = newarray (java.lang.Object)[2];

        $r10[0] = "error.nosuch.fileordir";

        $r10[1] = "{0} : no such file or directory";

        $r0[9] = $r10;

        $r11 = newarray (java.lang.Object)[2];

        $r11[0] = "error.write.file";

        $r11[1] = "Error in writing existing jar file";

        $r0[10] = $r11;

        $r12 = newarray (java.lang.Object)[2];

        $r12[0] = "out.added.manifest";

        $r12[1] = "added manifest";

        $r0[11] = $r12;

        $r13 = newarray (java.lang.Object)[2];

        $r13[0] = "out.adding";

        $r13[1] = "adding: {0}";

        $r0[12] = $r13;

        $r14 = newarray (java.lang.Object)[2];

        $r14[0] = "out.create";

        $r14[1] = "  created: {0}";

        $r0[13] = $r14;

        $r15 = newarray (java.lang.Object)[2];

        $r15[0] = "out.deflated";

        $r15[1] = "(deflated {0}%)";

        $r0[14] = $r15;

        $r16 = newarray (java.lang.Object)[2];

        $r16[0] = "out.extracted";

        $r16[1] = "extracted: {0}";

        $r0[15] = $r16;

        $r17 = newarray (java.lang.Object)[2];

        $r17[0] = "out.ignore.entry";

        $r17[1] = "ignoring entry {0}";

        $r0[16] = $r17;

        $r18 = newarray (java.lang.Object)[2];

        $r18[0] = "out.inflated";

        $r18[1] = " inflated: {0}";

        $r0[17] = $r18;

        $r19 = newarray (java.lang.Object)[2];

        $r19[0] = "out.size";

        $r19[1] = "(in = {0}) (out= {1})";

        $r0[18] = $r19;

        $r20 = newarray (java.lang.Object)[2];

        $r20[0] = "out.stored";

        $r20[1] = "(stored 0%)";

        $r0[19] = $r20;

        $r21 = newarray (java.lang.Object)[2];

        $r21[0] = "out.update.manifest";

        $r21[1] = "updated manifest";

        $r0[20] = $r21;

        $r22 = newarray (java.lang.Object)[2];

        $r22[0] = "usage";

        $r22[1] = "Usage: jar {ctxui}[vfmn0PMe] [jar-file] [manifest-file] [entry-point] [-C dir] files ...\nOptions:\n    -c  create new archive\n    -t  list table of contents for archive\n    -x  extract named (or all) files from archive\n    -u  update existing archive\n    -v  generate verbose output on standard output\n    -f  specify archive file name\n    -m  include manifest information from specified manifest file\n    -n  perform Pack200 normalization after creating a new archive\n    -e  specify application entry point for stand-alone application \n        bundled into an executable jar file\n    -0  store only; use no ZIP compression\n    -P  preserve leading \'/\' (absolute path) and \"..\" (parent directory) components from file names\n    -M  do not create a manifest file for the entries\n    -i  generate index information for the specified jar files\n    -C  change to the specified directory and include the following file\nIf any file is a directory then it is processed recursively.\nThe manifest file name, the archive file name and the entry point name are\nspecified in the same order as the \'m\', \'f\' and \'e\' flags.\n\nExample 1: to archive two class files into an archive called classes.jar: \n       jar cvf classes.jar Foo.class Bar.class \nExample 2: use an existing manifest file \'mymanifest\' and archive all the\n           files in the foo/ directory into \'classes.jar\': \n       jar cvfm classes.jar mymanifest -C foo/ .\n";

        $r0[21] = $r22;

        return $r0;
    }
}
