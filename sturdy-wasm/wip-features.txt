high prio:
-add parsing of location expressions
-analyze simple examples (no optimizations: how does the c-stack behave compared to with optimizations)
-add interpreter for (basic) dwarf location expressions


low priority:
- add support for relative path creation of DWARFContext object (DWARFContext::createFromFile function)
- optimize integer to enum conversion (currently O(n))
- add ranges to lexical blocks


interesting:    mandelbrot.log pixel_Group_i variable has a complicated location expression.
                This is about as complex as I have seen.
                And needs to be supported at some point

                DW_OP_implicit_location -> means, that the variable has no location and was instead inlined as a constant