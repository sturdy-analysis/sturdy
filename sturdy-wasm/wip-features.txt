high prio:
implement native function "getLocations" for DWARFDie class -> done
now: implement evaluation of locationExpressions

- support unnamed structs defined using "typedef struct <structdef without name> <typedef name>"

low priority:
- add support for relative path creation of DWARFContext object (DWARFContext::createFromFile function)
- optimize integer to enum conversion (currently O(n))