general notes:

structs that are passed by value are not copied into the stack frame of the called function.
instead: a copy is created in the callers' stack frame and then the struct is passed in a wasm local containing a pointer to the copy

the call by value semantics are not violated, but handling a pass by reference and pass by value struct are very similar